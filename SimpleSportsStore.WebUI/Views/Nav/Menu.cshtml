@model IEnumerable<string>

@Html.ActionLink("Home", "List", "Product")

@foreach (var link in Model) 
{
    @Html.RouteLink(
        link, 
        new {
            controller = "Product",
            action = "List",
            category = link,
            page = 1},
        new { @class = link == ViewBag.SelectedCategory ? "selected" : null }
        )   // @class is a parameter name, not a C# keyword
            // If the link is selected, the CSS class "selected" will be used
}

@*  Notes: 
    Html.RouteLink returns an anchor element
    Parameter 1: String that will be displayed as the link text
    Parameter 2: Object (untyped) containing route values: 
                 controller and action are self-explanatory,
                 category is a URL parameter name,
                 page is a URL parameter name

    The anchor element will look like this:
    <a href="/Soccer">Soccer</a>

    The anchor would be: <a href="/Soccer/Page1">Soccer</a>, 
          but (I think) the RouteConfig modifies it.
*@